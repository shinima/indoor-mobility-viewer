* {
  box-sizing border-box
}

body {
  margin 0
}

// todo 仅在storybook中, 该样式才有效
#root {
  width 100vw
  height 100vh
  position relative
}

body, button, input, optgroup, select, textarea {
  font-family 'microsoft yahei'
}
